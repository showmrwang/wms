<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="com.baozun.scm.primservice.whoperation.dao.handover.HandoverDao">
	<resultMap id="HandoverResultExt" type="com.baozun.scm.primservice.whoperation.model.handover.Handover">
	</resultMap>
	<resultMap id="HandoverCommandExt" type="com.baozun.scm.primservice.whoperation.command.handover.HandoverCommand">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="handoverColumnsExt">
	    <![CDATA[
	      	id as id,
	        	handover_station_id as handoverStationId,
	        	handover_station_type as handoverStationType,
	        	handover_batch as handoverBatch,
	        	group_condition as groupCondition,
	        	total_box as totalBox,
	        	customer_code as customerCode,
	        	customer_name as customerName,
	        	store_code as storeCode,
	        	store_name as storeName,
	        	transport_code as transportCode,
	        	transport_name as transportName,
	        	total_calc_weight as totalCalcWeight,
	        	total_actual_weight as totalActualWeight,
	        	ou_id as ouId,
	        	create_id as createId,
	        	create_time as createTime,
	        	last_modify_time as lastModifyTime,
	        	modified_id as modifiedId
	    ]]>
	</sql>
	
	<sql id="handoverDynamicWhereExt">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and id = #{id}
				</if>
	       
				<if test="handoverStationId!=null">
					and handover_station_id = #{handoverStationId}
				</if>
	       
				<if test="handoverStationType!=null">
					and handover_station_type = #{handoverStationType}
				</if>
	       
	       
		       <if test="handoverBatch!=null and handoverBatch!=''">
					and handover_batch = #{handoverBatch}
				</if>
	       
	       
		       <if test="groupCondition!=null and groupCondition!=''">
					and group_condition = #{groupCondition}
				</if>
	       
				<if test="totalBox!=null">
					and total_box = #{totalBox}
				</if>
	       
	       
		       <if test="customerCode!=null and customerCode!=''">
					and customer_code = #{customerCode}
				</if>
	       
	       
		       <if test="customerName!=null and customerName!=''">
					and customer_name = #{customerName}
				</if>
	       
	       
		       <if test="storeCode!=null and storeCode!=''">
					and store_code = #{storeCode}
				</if>
	       
	       
		       <if test="storeName!=null and storeName!=''">
					and store_name = #{storeName}
				</if>
	       
	       
		       <if test="transportCode!=null and transportCode!=''">
					and transport_code = #{transportCode}
				</if>
	       
	       
		       <if test="transportName!=null and transportName!=''">
					and transport_name = #{transportName}
				</if>
	       
				<if test="totalCalcWeight!=null">
					and total_calc_weight = #{totalCalcWeight}
				</if>
	       
				<if test="totalActualWeight!=null">
					and total_actual_weight = #{totalActualWeight}
				</if>
	       
				<if test="ouId!=null">
					and ou_id = #{ouId}
				</if>
	       
				<if test="createId!=null">
					and create_id = #{createId}
				</if>
	       
				<if test="createTime!=null">
					and create_time = #{createTime}
				</if>
	       
				<if test="lastModifyTime!=null">
					and last_modify_time = #{lastModifyTime}
				</if>
	       
				<if test="modifiedId!=null">
					and modified_id = #{modifiedId}
				</if>
		</where>
	</sql>
	
	 <select id="findByBatch"  resultType="com.baozun.scm.primservice.whoperation.model.handover.Handover" >
			select <include refid="handoverColumnsExt" />
		    <![CDATA[
			    from t_wh_handover 
		        where 
		        handover_batch = #{batch} 
		        limit 1
		    ]]>
		</select>	
		
		<select id="findAllUser" resultType="Long">
	select
	 distinct(u.create_id) as id
	from t_wh_handover u
	where u.ou_id =#{ouId}
	</select>
	
	
	
	
	    <select id="findListCountByQueryMapExt" resultType="long">
        select count(*) from  t_wh_handover handover 
        left join t_wh_handover_station  station on handover.handover_station_id =station.id
 		left join oper_user as user on handover.create_id = user.id 
		<include refid="handoverCommandDynamicWhereExt"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findListByQueryMapWithPageExt" resultMap="HandoverCommandExt">
    	select <include refid="handoverCommandColumnsExt" />
	    from t_wh_handover handover 
	    left join t_wh_handover_station  station on handover.handover_station_id =station.id
 		left join oper_user as user on handover.create_id = user.id 
		<include refid="handoverCommandDynamicWhereExt"/>
		order by handover.create_time DESC
    </select>
	<!-- 用于select查询公用抽取的列 -->
	<sql id="handoverCommandColumnsExt">
	    <![CDATA[
	      	handover.id as id,
	     	handover.handover_batch as handoverBatch ,
			station.code as handoverStationCode,
			handover.total_box as totalBox,
			handover.create_time as createTime,
			user.user_name as userName

	    ]]>
	</sql>
	
	<sql id="handoverCommandDynamicWhereExt">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and handover.id = #{id}
				</if>
	       
				<if test="handoverStationId!=null">
					and handover.handover_station_id = #{handoverStationId}
				</if>
	       
				 
	       
		       <if test="handoverBatch!=null and handoverBatch!=''">
					and handover.handover_batch = #{handoverBatch}
				</if>
	       
				<if test="ouId!=null">
					and handover.ou_id = #{ouId}
				</if>
	       
				<if test="createId!=null">
					and handover.create_id = #{createId}
				</if>
	       
				<if test="createTime!=null">
					and handover.create_time = #{createTime}
				</if>
	       
				<if test="lastModifyTime!=null">
					and handover.last_modify_time = #{lastModifyTime}
				</if>
	       
				<if test="modifiedId!=null">
					and handover.modified_id = #{modifiedId}
				</if>
		</where>
	</sql>
	
	 <select id="getBatchById" resultType="com.baozun.scm.primservice.whoperation.command.handover.HandoverCommand">
    	select <include refid="handoverCommandColumnsExt" />
	    from t_wh_handover handover 
	    left join t_wh_handover_station  station on handover.handover_station_id =station.id
 		left join oper_user as user on handover.create_id = user.id 
		where 	handover.id = #{id} 
				and handover.ou_id = #{ouId}
	
    </select>
	
	
	
	
	
	
</mapper>
