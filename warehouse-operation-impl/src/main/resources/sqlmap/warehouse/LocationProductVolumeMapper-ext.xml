<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="com.baozun.scm.primservice.whoperation.dao.warehouse.LocationProductVolumeDao">
    <resultMap id="LocationProductVolumeResultExt" type="com.baozun.scm.primservice.whoperation.model.warehouse.LocationProductVolume">
    </resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="locationProductVolumeColumnsExt">
        <![CDATA[
                id as id,
                pc_id as pcId,
                size as size,
                unit as unit,
                volume as volume,
                ou_id as ouId
        ]]>
    </sql>

    <sql id="locationProductVolumeDynamicWhereExt">
        <!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
        <where>

            <if test="id!=null">
                and id = #{id}
            </if>

            <if test="pcId!=null">
                and pc_id = #{pcId}
            </if>


            <if test="size!=null and size!=''">
                and size = #{size}
            </if>


            <if test="unit!=null and unit!=''">
                and unit = #{unit}
            </if>

            <if test="volume!=null">
                and volume = #{volume}
            </if>

            <if test="ouId!=null">
                and ou_id = #{ouId}
            </if>
        </where>
    </sql>

    <delete id="deleteExt" parameterType="java.lang.Long">
        delete from t_wh_location_product_volume
        <where>
            <if test="id!=null">
                and id = #{id}
            </if>
            <if test="pcId!=null">
                pc_id = #{pcId}
            </if>
            and ou_id = #{ouId}
        </where>
    </delete>

</mapper>
