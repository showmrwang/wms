<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="com.baozun.scm.primservice.whoperation.dao.warehouse.inventory.WhSkuInventorySnDao">
	<resultMap id="WhSkuInventorySnResult" type="com.baozun.scm.primservice.whoperation.model.warehouse.inventory.WhSkuInventorySn">
	</resultMap>
	<resultMap id="WhSkuInventorySnPaginationResult" type="lark.common.dao.Pagination">
    	<collection property="items" javaType="ArrayList" resultMap="WhSkuInventorySnResult"/>
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="whSkuInventorySnColumns">
	    <![CDATA[
	        	id as id,
	        	sn as sn,
	        	occupation_code as occupationCode,
	        	defect_ware_barcode as defectWareBarcode,
	        	defect_source as defectSource,
	        	defect_type_id as defectTypeId,
	        	defect_reasons_id as defectReasonsId,
	        	status as status,
	        	inv_attr as invAttr,
	        	uuid as uuid,
	        	ou_id as ouId,
	        	sys_uuid as sysUuid
	    ]]>
	</sql>
	<sql id="whSkuInventorySnConditionWhereExt">
		<where>
			<if test="likeEqualsMap != null">
				<foreach collection="likeEqualsMap" index="key"  item="value" separator="and" open=" and "> 
					${key} like CONCAT('%',#{value},'%') 
				</foreach> 
			</if> 
			<if test="equalsMap != null"> 
				<foreach collection="equalsMap" index="key"  item="value" separator="and" open=" and "> 
					${key} = #{value} 
				</foreach> 
			</if> 
			<if test="notEqualsMap != null"> 
				<foreach collection="notEqualsMap" index="key"  item="value" separator="and" open=" and "> 
					${key} != #{value} 
				</foreach> 
			</if> 
			<if test="greateMap != null"> 
				<foreach collection="greateMap" index="key"  item="value" separator="and" open=" and "> 
					${key} &gt; #{value} 
				</foreach> 
			</if> 
			<if test="greateEqualsMap != null"> 
				<foreach collection="greateEqualsMap" index="key"  item="value" separator="and" open=" and "> 
					${key} &gt;= #{value} 
				</foreach> 
			</if> 
			<if test="lessMap != null"> 
				<foreach collection="lessMap" index="key"  item="value" separator="and" open=" and "> 
					${key} &lt; #{value} 
				</foreach> 
			</if> 
			<if test="lessEqualsMap != null"> 
				<foreach collection="lessEqualsMap" index="key"  item="value" separator="and" open=" and "> 
					${key} &lt;= #{value} 
				</foreach> 
			</if> 
			<if test="nullMap != null"> 
				<foreach collection="nullMap" index="key"  item="value" separator="and" open=" and "> 
					${key} is null 
				</foreach> 
			</if> 
			<if test="notNullMap != null"> 
				<foreach collection="notNullMap" index="key"  item="value" separator="and" open=" and "> 
					${key} is not null 
				</foreach> 
			</if> 
			<if test="inMap != null"> 
				<foreach collection="inMap" index="key"  item="value" separator="and" open=" and (" close=")"> 
					<foreach collection="value" item="itemin" index="index" separator="or"> 
						${key} = #{itemin} 
					</foreach> 
				</foreach> 
			</if> 
			<if test="notInMap != null"> 
				<foreach collection="notInMap" index="key"  item="value" separator="and" open=" and (" close=")"> 
					<foreach collection="value" item="itemin" index="index" separator="and"> 
						${key} != #{itemin} 
					</foreach> 
				</foreach> 
			</if> 
		</where>
		<if test="sortBy != null"> 
			<foreach collection="sortBy" item="item" separator=","  open=" order by "> 
				${item.field} ${item.type}
			</foreach> 
		</if>
	</sql>
	
		<!-- 如果有自增字段 -->
		
			<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
			<insert id="insert" parameterType="com.baozun.scm.primservice.whoperation.model.warehouse.inventory.WhSkuInventorySn" 	
			 flushCache="true">
		    <![CDATA[
		        INSERT INTO
		        t_wh_sku_inventory_sn (
		        	id ,
		        	sn ,
		        	occupation_code ,
		        	defect_ware_barcode ,
		        	defect_source ,
		        	defect_type_id ,
		        	defect_reasons_id ,
		        	status ,
		        	inv_attr ,
		        	uuid ,
		        	ou_id ,
		        	sys_uuid
		        ) VALUES (
		     	    #{id,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        	#{sn,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        	#{occupationCode,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        	#{defectWareBarcode,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        	#{defectSource,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        	#{defectTypeId,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        	#{defectReasonsId,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        	#{status,javaType=java.lang.Integer,jdbcType=INTEGER} ,
		        	#{invAttr,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        	#{uuid,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        	#{ouId,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        	#{sysUuid,javaType=java.lang.String,jdbcType=VARCHAR} 
		        )
		    ]]>

		</insert>
		
		
		<update id="update" parameterType="com.baozun.scm.primservice.whoperation.model.warehouse.inventory.WhSkuInventorySn">
	    <![CDATA[
	        UPDATE t_wh_sku_inventory_sn SET
		        sn = #{sn,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        occupation_code = #{occupationCode,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        defect_ware_barcode = #{defectWareBarcode,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        defect_source = #{defectSource,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        defect_type_id = #{defectTypeId,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        defect_reasons_id = #{defectReasonsId,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        status = #{status,javaType=java.lang.Integer,jdbcType=INTEGER} ,
		        inv_attr = #{invAttr,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        uuid = #{uuid,javaType=java.lang.String,jdbcType=VARCHAR} ,
		        sys_uuid = #{sysUuid,javaType=java.lang.String,jdbcType=VARCHAR} 
	        WHERE 
		        id = #{id} 
		        and ou_id = #{ouId}
	    ]]>
		</update>
		<update id="batchUpdate">
	        UPDATE t_wh_sku_inventory_sn SET
	        	<foreach collection="batchUpdateMap" index="key"  item="value" separator=",">
	        		${key} = #{value}
	        	</foreach>
	        	<include refid="whSkuInventorySnConditionWhereExt"/>
		</update>
		
	   <delete id="delete" parameterType="java.lang.Long">
	    <![CDATA[
	        delete from t_wh_sku_inventory_sn where
		        id = #{id} 
	    ]]>
	    </delete> 
	    
	     <delete id="deleteByIds" parameterType="list">
	    
	        delete from t_wh_sku_inventory_sn 
	        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>	
	  
	    </delete>  
	    <update id="batchDelete">
	        delete from t_wh_sku_inventory_sn <include refid="whSkuInventorySnConditionWhereExt"/>
		</update>
	         
	    <select id="findById" parameterType="java.lang.Long" resultMap="WhSkuInventorySnResult" flushCache="false">
			select <include refid="whSkuInventorySnColumns" />
		    <![CDATA[
			    from t_wh_sku_inventory_sn 
		        where 
		        id = #{id} 
		    ]]>
		</select>	
		<select id="getByIds" parameterType="list"  resultMap="WhSkuInventorySnResult" flushCache="false">
			select <include refid="whSkuInventorySnColumns" />
			    from t_wh_sku_inventory_sn 
		        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>					
		</select>	
	<sql id="whSkuInventorySnDynamicWhere">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and id = #{id}
				</if>	       
	       
		       <if test="sn!=null and sn!=''">
					and sn = #{sn}
				</if>
	       
	       
		       <if test="occupationCode!=null and occupationCode!=''">
					and occupation_code = #{occupationCode}
				</if>
	       
	       
		       <if test="defectWareBarcode!=null and defectWareBarcode!=''">
					and defect_ware_barcode = #{defectWareBarcode}
				</if>
	       
	       
		       <if test="defectSource!=null and defectSource!=''">
					and defect_source = #{defectSource}
				</if>
	       
				<if test="defectTypeId!=null">
					and defect_type_id = #{defectTypeId}
				</if>
	       
				<if test="defectReasonsId!=null">
					and defect_reasons_id = #{defectReasonsId}
				</if>
	       
				<if test="status!=null">
					and status = #{status}
				</if>
	       
	       
		       <if test="invAttr!=null and invAttr!=''">
					and inv_attr = #{invAttr}
				</if>
	       
	       
		       <if test="uuid!=null and uuid!=''">
					and uuid = #{uuid}
				</if>
	       
				<if test="ouId!=null">
					and ou_id = #{ouId}
				</if>
		</where>
	</sql>
	

	<select id="getCount" resultType="long">
        select count(*) from t_wh_sku_inventory_sn 
    </select>
    <select id="findListCountByQueryMap" resultType="long">
        select count(*) from t_wh_sku_inventory_sn 
		<include refid="whSkuInventorySnDynamicWhere"/>    
    </select>
    
    <select id="findListCountByParam" resultType="long">
        select count(*) from t_wh_sku_inventory_sn 
		<include refid="whSkuInventorySnDynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findListByQueryMapWithPage" resultMap="WhSkuInventorySnResult">
    	select <include refid="whSkuInventorySnColumns" />
	    from t_wh_sku_inventory_sn 
		<include refid="whSkuInventorySnDynamicWhere"/>
		
    </select>
    
    <select id="findListByQueryMap" resultMap="WhSkuInventorySnResult">
    	select <include refid="whSkuInventorySnColumns" />
	    from t_wh_sku_inventory_sn 
		<include refid="whSkuInventorySnDynamicWhere"/>
		
    </select>
    
    <select id="findListByParam" resultMap="WhSkuInventorySnResult">
    	select <include refid="whSkuInventorySnColumns" />
	    from t_wh_sku_inventory_sn 
		<include refid="whSkuInventorySnDynamicWhere"/>
		
    </select>
    
    <select id="query" resultMap="WhSkuInventorySnResult">
		select <include refid="whSkuInventorySnColumns" />
	    from t_wh_sku_inventory_sn  
		<include refid="whSkuInventorySnConditionWhereExt"/>
    </select>
    
    <select id="queryCount" resultType="long">
		select count(*) from t_wh_sku_inventory_sn  
		<include refid="whSkuInventorySnConditionWhereExt"/>
    </select>

	
</mapper>
