<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="com.baozun.scm.primservice.whoperation.dao.warehouse.InventoryStatusDao">
	<resultMap id="InventoryStatusResultExt" type="com.baozun.scm.primservice.whoperation.model.warehouse.InventoryStatus">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="inventoryStatusColumnsExt">
	    <![CDATA[
	        	id as id,
	        	name as name,
	        	is_for_sale as isForSale,
	        	is_in_cost as isInCost,
	        	description as description,
	        	lifecycle as lifecycle,
	        	is_defective as isDefective
	        	
	    ]]>
	</sql>
	
	<sql id="inventoryStatusDynamicWhereExt">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and id = #{id}
				</if>
	       
	       
		       <if test="name!=null and name!=''">
					and name = #{name}
				</if>
	       
				<if test="isForSale!=null">
					and is_for_sale = #{isForSale}
				</if>
				
				<if test="isDefective!=null">
					and is_defective = #{isDefective}
				</if>
	       
				<if test="isInCost!=null">
					and is_in_cost = #{isInCost}
				</if>
	       
	       
		       <if test="description!=null and description!=''">
					and description = #{description}
				</if>
	       
				<if test="lifecycle!=null">
					and lifecycle = #{lifecycle}
				</if>
		</where>
	</sql>
	
	<select id="findInventoryStatusById" resultMap="InventoryStatusResultExt">
		select <include refid="inventoryStatusColumnsExt" />
		from t_wh_inventory_status 
		where 
		id = #{id} 
	</select>
	
	<select id="findInventoryStatus" resultMap="InventoryStatusResultExt">
		select <include refid="inventoryStatusColumnsExt" />
		from t_wh_inventory_status 
	</select>
	
	<select id="findInventoryStatusByName" resultMap="InventoryStatusResultExt">
    	select <include refid="inventoryStatusColumnsExt" />
	    from t_wh_inventory_status 
	    where 
	    name = #{name}
    </select>
    
</mapper>
