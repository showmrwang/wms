<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="com.baozun.scm.primservice.whoperation.dao.warehouse.WhOutInventoryboxRelationshipDao">
    <resultMap id="WhOutInventoryboxRelationshipResultExt" type="com.baozun.scm.primservice.whoperation.model.warehouse.WhOutInventoryboxRelationship">
    </resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="whOutInventoryboxRelationshipColumnsExt">
        <![CDATA[
                id as id,
                outInventoryBox_id as outInventoryBoxId,
                type as type,
                relation_id as relationId,
                ou_id as ouId
        ]]>
    </sql>

    <sql id="whOutInventoryboxRelationshipDynamicWhereExt">
        <!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
        <where>

            <if test="id!=null">
                and id = #{id}
            </if>

            <if test="outInventoryBoxId!=null">
                and outInventoryBox_id = #{outInventoryBoxId}
            </if>

            <if test="type!=null and type!=''">
                and type = #{type}
            </if>

            <if test="relationId!=null">
                and relation_id = #{relationId}
            </if>

            <if test="ouId!=null">
                and ou_id = #{ouId}
            </if>
        </where>
    </sql>


    <select id="getRelationshipByType" resultMap="WhOutInventoryboxRelationshipResultExt">
        select
            <include refid="whOutInventoryboxRelationshipColumnsExt" />
        from
            t_wh_outboundbox_relationship
        where
            ou_id = #{ouId}
            and relation_id = #{relationshipId}
            and type = #{type}
    </select>

    <select id="getGeneralRelationship" resultMap="WhOutInventoryboxRelationshipResultExt">
        select
            <include refid="whOutInventoryboxRelationshipColumnsExt" />
        from
            t_wh_outboundbox_relationship
        where
            ou_id = #{ouId}
            and relation_id is null
            and type is null
    </select>


</mapper>
