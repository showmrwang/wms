<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="com.baozun.scm.primservice.whoperation.dao.warehouse.RecommendRuleHeadDao">
    <resultMap id="RecommendRuleHeadResultExt"
               type="com.baozun.scm.primservice.whoperation.model.warehouse.RecommendRuleHead">
    </resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="recommendRuleHeadColumnsExt">
        <![CDATA[
                id as id,
                rule_sql_head as ruleSqlHead,
                rule_type as ruleType,
                create_time as createTime,
                created_id as createdId,
                last_modify_time as lastModifyTime,
                modified_id as modifiedId
        ]]>
    </sql>

    <sql id="recommendRuleHeadDynamicWhereExt">
        <!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
        <where>

            <if test="id!=null">
                and id = #{id}
            </if>


            <if test="ruleSqlHead!=null and ruleSqlHead!=''">
                and rule_sql_head = #{ruleSqlHead}
            </if>


            <if test="ruleType!=null and ruleType!=''">
                and rule_type = #{ruleType}
            </if>

            <if test="createTime!=null">
                and create_time = #{createTime}
            </if>

            <if test="createdId!=null">
                and created_id = #{createdId}
            </if>

            <if test="lastModifyTime!=null">
                and last_modify_time = #{lastModifyTime}
            </if>

            <if test="modifiedId!=null">
                and modified_id = #{modifiedId}
            </if>
        </where>
    </sql>

    <select id="getRecommendRuleHeadByRuleType" resultMap="RecommendRuleHeadResultExt">
        select
        <include refid="recommendRuleHeadColumnsExt"/>
        from t_wh_recommend_rule_head
        where rule_type = #{ruleType}
    </select>

</mapper>
