<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="com.baozun.scm.primservice.whoperation.dao.poasn.PoAsnOuDao">
	<resultMap id="PoAsnOuResult" type="com.baozun.scm.primservice.whoperation.model.poasn.PoAsnOu">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="poAsnOuColumns">
	    <![CDATA[
	        	id as id,
	        	po_id as poId,
	        	ou_id as ouId
	    ]]>
	</sql>
	
		<!-- 如果有自增字段 -->
	
			<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
			<insert id="insert" parameterType="com.baozun.scm.primservice.whoperation.model.poasn.PoAsnOu" 
				useGeneratedKeys="true" keyProperty="id" 
			 flushCache="true">
		    <![CDATA[
		        INSERT INTO
		        t_wh_poasn_ou (
		        	po_id ,
		        	ou_id 
		        ) VALUES (
		        	#{poId,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        	#{ouId,javaType=java.lang.Long,jdbcType=BIGINT} 
		        )
		    ]]>
	
			</insert>
		
		<update id="update" parameterType="com.baozun.scm.primservice.whoperation.model.poasn.PoAsnOu">
	    <![CDATA[
	        UPDATE t_wh_poasn_ou SET
		        po_id = #{poId,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        ou_id = #{ouId,javaType=java.lang.Long,jdbcType=BIGINT} 
	        WHERE 
		        id = #{id} 
	    ]]>
		</update>
	   <delete id="delete" parameterType="java.lang.Long">
	    <![CDATA[
	        delete from t_wh_poasn_ou where
		        id = #{id} 
	    ]]>
	    </delete> 
	    
	     <delete id="deleteByIds" parameterType="list">
	    
	        delete from t_wh_poasn_ou 
	        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>	
	  
	    </delete>  
	         
	    <select id="findById" parameterType="java.lang.Long" resultMap="PoAsnOuResult" flushCache="false">
			select <include refid="poAsnOuColumns" />
		    <![CDATA[
			    from t_wh_poasn_ou 
		        where 
		        id = #{id} 
		    ]]>
		</select>	
		<select id="getByIds" parameterType="list"  resultMap="PoAsnOuResult" flushCache="false">
			select <include refid="poAsnOuColumns" />
			    from t_wh_poasn_ou 
		        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>					
		</select>	
	<sql id="poAsnOuDynamicWhere">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and id = #{id}
				</if>
	       
				<if test="poId!=null">
					and po_id = #{poId}
				</if>
	       
				<if test="ouId!=null">
					and ou_id = #{ouId}
				</if>
		</where>
	</sql>
	

	<select id="getCount" resultType="long">
        select count(*) from t_wh_poasn_ou 
    </select>
    <select id="findListCountByQueryMap" resultType="long">
        select count(*) from t_wh_poasn_ou 
		<include refid="poAsnOuDynamicWhere"/>    
    </select>
    
    <select id="findListCountByParam" resultType="long">
        select count(*) from t_wh_poasn_ou 
		<include refid="poAsnOuDynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findListByQueryMapWithPage" resultMap="PoAsnOuResult">
    	select <include refid="poAsnOuColumns" />
	    from t_wh_poasn_ou 
		<include refid="poAsnOuDynamicWhere"/>
		
    </select>
    
    <select id="findListByQueryMap" resultMap="PoAsnOuResult">
    	select <include refid="poAsnOuColumns" />
	    from t_wh_poasn_ou 
		<include refid="poAsnOuDynamicWhere"/>
		
    </select>
    
    <select id="findListByParam" resultMap="PoAsnOuResult">
    	select <include refid="poAsnOuColumns" />
	    from t_wh_poasn_ou 
		<include refid="poAsnOuDynamicWhere"/>
		
    </select>

	
</mapper>
