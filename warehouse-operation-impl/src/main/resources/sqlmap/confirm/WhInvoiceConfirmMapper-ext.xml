<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="com.baozun.scm.primservice.whoperation.dao.confirm.WhInvoiceConfirmDao">
	<resultMap id="WhInvoiceConfirmResultExt" type="com.baozun.scm.primservice.whoperation.model.confirm.WhInvoiceConfirm">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="whInvoiceConfirmColumnsExt">
	    <![CDATA[
	        	id as id,
	        	invoice_code as invoiceCode,
	        	invoice_no as invoiceNo,
	        	transport_service_providers as transportServiceProviders,
	        	tracking_number as trackingNumber,
	        	ou_id as ouId,
	        	create_time as createTime,
	        	data_source as dataSource
	    ]]>
	</sql>
	
	<sql id="whInvoiceConfirmDynamicWhereExt">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and id = #{id}
				</if>
	       
	       
		       <if test="invoiceCode!=null and invoiceCode!=''">
					and invoice_code = #{invoiceCode}
				</if>
	       
	       
		       <if test="invoiceNo!=null and invoiceNo!=''">
					and invoice_no = #{invoiceNo}
				</if>
	       
	       
		       <if test="transportServiceProviders!=null and transportServiceProviders!=''">
					and transport_service_providers = #{transportServiceProviders}
				</if>
	       
	       
		       <if test="trackingNumber!=null and trackingNumber!=''">
					and tracking_number = #{trackingNumber}
				</if>
	       
				<if test="ouId!=null">
					and ou_id = #{ouId}
				</if>
	       
				<if test="createTime!=null">
					and create_time = #{createTime}
				</if>
	       
	       
		       <if test="dataSource!=null and dataSource!=''">
					and data_source = #{dataSource}
				</if>
		</where>
	</sql>
	
		<sql id="wmsInvoiceConfirmColumnsExt">
	    <![CDATA[
	        	invoice_code as invoiceCode,
	        	invoice_no as invoiceNo,
	        	transport_service_providers as transportServiceProviders,
	        	tracking_number as trackingNumber,
	        	data_source as dataSource
	    ]]>
	</sql>
	
	<resultMap id="WmsInvoiceConfirmResultExt" type="com.baozun.scm.primservice.whinterface.model.inventory.WmsInvoiceConfirm">
	</resultMap>
	
	<select id="findWmsInvoiceConfirmByCreateTimeAndDataSource" resultMap="WmsInvoiceConfirmResultExt">
		select <include refid="wmsInvoiceConfirmColumnsExt" />
		from t_wh_invoice_confirm 
		where 
		create_time &gt; #{beginTime} 
		and create_time &lt;= #{endTime} 
		and data_source = #{dataSource}
		and ou_id = #{ouid}
		order by id asc
		limit #{start},#{pageSize}
	</select>
	
</mapper>
