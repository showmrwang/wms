<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.baozun.scm.primservice.whoperation.dao.odo.WhOdoInvoiceLineDao">
    <resultMap id="WhOdoInvoiceLineResult" type="com.baozun.scm.primservice.whoperation.model.odo.WhOdoInvoiceLine">
    </resultMap>

    <resultMap id="WhOdoInvoiceLinePaginationResult" type="lark.common.dao.Pagination">
        <collection property="items" javaType="ArrayList" resultMap="WhOdoInvoiceLineResult"/>
    </resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="whOdoInvoiceLineColumns">
        <![CDATA[
                id as id,
                odo_invoice_id as odoInvoiceId,
                linenum as linenum,
                qty as qty,
                unit_price as unitPrice,
                amt as amt,
                item as item,
                ou_id as ouId
        ]]>
    </sql>
    <sql id="whOdoInvoiceLineConditionWhereExt">
        <where>
            <if test="likeEqualsMap != null">
                <foreach collection="likeEqualsMap" index="key" item="value" separator="and" open=" and ">
                    ${key} like CONCAT('%',#{value},'%')
                </foreach>
            </if>

            <if test="equalsMap != null">
                <foreach collection="equalsMap" index="key" item="value" separator="and" open=" and ">
                    ${key} = #{value}
                </foreach>
            </if>

            <if test="notEqualsMap != null">
                <foreach collection="notEqualsMap" index="key" item="value" separator="and" open=" and ">
                    ${key} != #{value}
                </foreach>
            </if>

            <if test="greateMap != null">
                <foreach collection="greateMap" index="key" item="value" separator="and" open=" and ">
                    ${key} &gt; #{value}
                </foreach>
            </if>

            <if test="greateEqualsMap != null">
                <foreach collection="greateEqualsMap" index="key" item="value" separator="and" open=" and ">
                    ${key} &gt;= #{value}
                </foreach>
            </if>

            <if test="lessMap != null">
                <foreach collection="lessMap" index="key" item="value" separator="and" open=" and ">
                    ${key} &lt; #{value}
                </foreach>
            </if>

            <if test="lessEqualsMap != null">
                <foreach collection="lessEqualsMap" index="key" item="value" separator="and" open=" and ">
                    ${key} &lt;= #{value}
                </foreach>
            </if>

            <if test="nullMap != null">
                <foreach collection="nullMap" index="key" item="value" separator="and" open=" and ">
                    ${key} is null
                </foreach>
            </if>

            <if test="notNullMap != null">
                <foreach collection="notNullMap" index="key" item="value" separator="and" open=" and ">
                    ${key} is not null
                </foreach>
            </if>

            <if test="inMap != null">
                <foreach collection="inMap" index="key" item="value" separator="and" open=" and (" close=")">
                    <foreach collection="value" item="itemin" index="index" separator="or">
                        ${key} = #{itemin}
                    </foreach>
                </foreach>
            </if>

            <if test="notInMap != null">
                <foreach collection="notInMap" index="key" item="value" separator="and" open=" and (" close=")">
                    <foreach collection="value" item="itemin" index="index" separator="and">
                        ${key} != #{itemin}
                    </foreach>
                </foreach>
            </if>
        </where>

        <if test="sortBy != null">
            <foreach collection="sortBy" item="item" separator="," open=" order by ">
                ${item.field} ${item.type}
            </foreach>
        </if>
    </sql>

    <!-- 如果有自增字段 -->

    <!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
    <insert id="insert" parameterType="com.baozun.scm.primservice.whoperation.model.odo.WhOdoInvoiceLine" flushCache="true">
        <![CDATA[
                INSERT INTO
                t_wh_odo_invoice_line (
                id,
                    odo_invoice_id ,
                    linenum ,
                    qty ,
                    unit_price ,
                    amt ,
                    item ,
                    ou_id 
                ) VALUES (
                #{id,javaType=java.lang.Long,jdbcType=BIGINT} ,
                    #{odoInvoiceId,javaType=java.lang.Long,jdbcType=BIGINT} ,
                    #{linenum,javaType=java.lang.String,jdbcType=VARCHAR} ,
                    #{qty,javaType=java.lang.Integer,jdbcType=INTEGER} ,
                    #{unitPrice,javaType=java.lang.Double,jdbcType=DECIMAL} ,
                    #{amt,javaType=java.lang.Double,jdbcType=DECIMAL} ,
                    #{item,javaType=java.lang.String,jdbcType=VARCHAR} ,
                    #{ouId,javaType=java.lang.Long,jdbcType=BIGINT} 
                )
            ]]>

    </insert>

    <update id="update" parameterType="com.baozun.scm.primservice.whoperation.model.odo.WhOdoInvoiceLine">
        <![CDATA[
            UPDATE t_wh_odo_invoice_line SET
                odo_invoice_id = #{odoInvoiceId,javaType=java.lang.Long,jdbcType=BIGINT} ,
                linenum = #{linenum,javaType=java.lang.String,jdbcType=VARCHAR} ,
                qty = #{qty,javaType=java.lang.Integer,jdbcType=INTEGER} ,
                unit_price = #{unitPrice,javaType=java.lang.Double,jdbcType=DECIMAL} ,
                amt = #{amt,javaType=java.lang.Double,jdbcType=DECIMAL} ,
                item = #{item,javaType=java.lang.String,jdbcType=VARCHAR} ,
                ou_id = #{ouId,javaType=java.lang.Long,jdbcType=BIGINT} 
            WHERE 
                id = #{id} 
        ]]>
    </update>

    <update id="batchUpdate">
        UPDATE t_wh_odo_invoice_line SET
        <foreach collection="batchUpdateMap" index="key" item="value" separator=",">
            ${key} = #{value}
        </foreach>
        <include refid="whOdoInvoiceLineConditionWhereExt"/>
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        <![CDATA[
            delete from t_wh_odo_invoice_line where
                id = #{id} 
        ]]>
    </delete>

    <delete id="deleteByIds" parameterType="list">

        delete from t_wh_odo_invoice_line
        where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>

    </delete>

    <update id="batchDelete">
        delete from t_wh_odo_invoice_line
        <include refid="whOdoInvoiceLineConditionWhereExt"/>
    </update>

    <select id="findById" parameterType="java.lang.Long" resultMap="WhOdoInvoiceLineResult" flushCache="false">
        select
        <include refid="whOdoInvoiceLineColumns"/>
        <![CDATA[
                from t_wh_odo_invoice_line 
                where 
                id = #{id} 
            ]]>
    </select>

    <select id="getByIds" parameterType="list" resultMap="WhOdoInvoiceLineResult" flushCache="false">
        select
        <include refid="whOdoInvoiceLineColumns"/>
        from t_wh_odo_invoice_line
        where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <sql id="whOdoInvoiceLineDynamicWhere">
        <!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
        <where>

            <if test="id!=null">
                and id = #{id}
            </if>

            <if test="odoInvoiceId!=null">
                and odo_invoice_id = #{odoInvoiceId}
            </if>

            <if test="linenum!=null and linenum!=''">
                and linenum = #{linenum}
            </if>

            <if test="qty!=null">
                and qty = #{qty}
            </if>

            <if test="unitPrice!=null">
                and unit_price = #{unitPrice}
            </if>

            <if test="amt!=null">
                and amt = #{amt}
            </if>

            <if test="item!=null and item!=''">
                and item = #{item}
            </if>

            <if test="ouId!=null">
                and ou_id = #{ouId}
            </if>
        </where>
    </sql>

    <select id="getCount" resultType="long">
        select count(*) from t_wh_odo_invoice_line
    </select>

    <select id="findListCountByQueryMap" resultType="long">
        select count(*) from t_wh_odo_invoice_line
        <include refid="whOdoInvoiceLineDynamicWhere"/>
    </select>

    <select id="findListCountByParam" resultType="long">
        select count(*) from t_wh_odo_invoice_line
        <include refid="whOdoInvoiceLineDynamicWhere"/>
    </select>

    <!--
        分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
        因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findListByQueryMapWithPage" resultMap="WhOdoInvoiceLineResult">
        select
        <include refid="whOdoInvoiceLineColumns"/>
        from t_wh_odo_invoice_line
        <include refid="whOdoInvoiceLineDynamicWhere"/>

    </select>

    <select id="findListByQueryMap" resultMap="WhOdoInvoiceLineResult">
        select
        <include refid="whOdoInvoiceLineColumns"/>
        from t_wh_odo_invoice_line
        <include refid="whOdoInvoiceLineDynamicWhere"/>

    </select>

    <select id="findListByParam" resultMap="WhOdoInvoiceLineResult">
        select
        <include refid="whOdoInvoiceLineColumns"/>
        from t_wh_odo_invoice_line
        <include refid="whOdoInvoiceLineDynamicWhere"/>

    </select>

    <select id="query" resultMap="WhOdoInvoiceLineResult">
        select
        <include refid="whOdoInvoiceLineColumns"/>
        from t_wh_odo_invoice_line
        <include refid="whOdoInvoiceLineConditionWhereExt"/>
    </select>

    <select id="queryCount" resultType="long">
        select count(*) from t_wh_odo_invoice_line
        <include refid="whOdoInvoiceLineConditionWhereExt"/>
    </select>

</mapper>
