package com.baozun.scm.primservice.whoperation.manager.odo.wave;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service("CreateWorkInWaveManager")
@Transactional
public class CreateWorkInWaveManagerImpl implements CreateWorkInWaveManager {
    
    protected static final Logger log = LoggerFactory.getLogger(CreateWorkInWaveManagerImpl.class);

    
}
